{
  "devDependencies": {
    "@parcel/transformer-sass": "^2.6.2",
    "parcel": "^2.6.2",
    "shx": "^0.3.2"
  },
  "dependencies": {
    "gh-pages": "^4.0.0",
    "gsap": "^3.10.4"
  },
  "scripts": {
    "start": "parcel ./*.html",
    "predeploy": "rm -rf dist && parcel build ./*.html --public-url https://github.com/mirjanaperic/frankachela",
    "deploy": "gh-pages -d dist"
}
}
